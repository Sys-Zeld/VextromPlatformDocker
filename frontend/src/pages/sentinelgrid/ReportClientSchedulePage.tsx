import ManagementReport from "../../components/sentinelgrid/ManagementReport";

// Cronograma por cliente: OMs agrupadas por cliente + site.
export default function ReportClientSchedulePage() {
  return <ManagementReport kind="client-schedule" />;
}
